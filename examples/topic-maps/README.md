# Topic Maps

Run with `python -m graph_model_examples run topic-maps`.

Topic Maps emphasize topics, associations, occurrences, and subject identity. The Acme duplicate topics merge because they share a subject identifier.

Elegant: subject identity is explicit. Awkward: fewer teams use Topic Maps tooling today, so integration often requires mapping to RDF, LPG, or documents.
