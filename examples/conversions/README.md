# Cross-model Conversion and Loss Audit

Run with `python -m graph_model_examples run conversions`.

The conversion layer uses a canonical intermediate representation of the Alice/Acme claim and records whether each target model preserves, transforms, or loses statement metadata.
