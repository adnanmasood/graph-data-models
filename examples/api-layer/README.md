# GraphQL API Layer

Run with `python -m graph_model_examples run api-layer`.

GraphQL describes the shape of API responses. It can sit over RDF, LPG, SQL, document stores, services, or in-memory fixtures. It does not determine whether the backend is RDF, LPG, hypergraph, or EAV.
