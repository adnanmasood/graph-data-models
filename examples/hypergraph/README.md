# Hypergraph Examples

Run with `make demo-hypergraph`.

The employment relation is a typed hyperedge connecting employee, employer, role, authority, location, source, and temporal attributes. A verification hyperedge points to the employment hyperedge.

Elegant: n-ary relationships stay intact. Awkward: many databases and query languages are optimized around binary edges, so projections are often needed.
