# Gotchas Labs

Run with `python -m graph_model_examples run gotchas`.

Labs cover open-world reasoning versus SHACL checks, blank nodes versus IRIs, reification verbosity, LPG supernodes, qualifier loss during flattening, bounded Datalog recursion, EAV join complexity, and vector retrieval versus graph facts.
