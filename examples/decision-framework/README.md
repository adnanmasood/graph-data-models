# Decision Framework

Run with `python -m graph_model_examples run decision-framework` or `python -m graph_model_examples recommend --semantic --provenance --graphrag`.

Use the checklist as a conversation starter. Real production systems still need architecture review, operational constraints, data governance review, and migration planning.
