# Query and API Languages

This directory keeps SPARQL, Cypher, Gremlin, GQL, and GraphQL examples side by side for the same Alice/Acme question.

GraphQL's name contains "graph", but it describes the shape of API responses. It can sit over RDF, LPG, SQL, document stores, services, or in-memory fixtures. It does not determine the backend data model.
