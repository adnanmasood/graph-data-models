# Contributing

Keep examples deterministic, offline by default, and tied to the canonical Alice/Acme fact unless a case study introduces its own data. Add behavior tests for executable examples and label external-engine snippets clearly.
